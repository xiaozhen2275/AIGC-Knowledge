# AI读书会知识库工程总纲与 Agent 初始化规范

适用对象：Claude Code、Codex、WorkBuddy，以及后续进入本项目的其他 AI agent。

本文件由 `AGENTS.md` 调用，是当前知识库工程的总纲文档。任何 agent 在读取、整理、修改、扩展本项目资料前，必须先阅读 `AGENTS.md`，再阅读本文件，最后进入具体文档操作。

## 1. 项目定位

本项目是《1000个铁粉，打造个人品牌的底层逻辑》三期线下 AI 读书演讲工作坊资料包。

它不是普通读书笔记仓库，也不是 AI 工具说明书，而是一套可用于线下交付的活动产品知识库。核心目标是：

- 让普通职场人在 2 小时内完成一段能讲出来的 3 分钟表达。
- 让学员从一本书里的一个观点出发，连接自己的理解、经历和表达。
- 让每场活动都沉淀一张可复用的知识资产卡。
- 为后续 AI 进阶使用、AI 个人知识库、知识资产沉淀做轻量承接。

所有新增和修改都要服务“现场可交付、学员可完成、主理人可执行”。

## 2. Agent 启动顺序

每次打开 Claude Code、Codex、WorkBuddy 或其他 AI agent 操作本项目时，按以下顺序加载上下文：

1. 先读 `AGENTS.md`。
2. 再读 `00_AGENT初始化总纲.md`。
3. 再读 `00_使用说明.md`。
4. 根据任务类型读取对应主文档：
   - 总体设计：读 `01_总方案.md`。
   - 第 1 期主持和流程：读 `02_第1期_读懂一本书_主持稿.md`。
   - 第 2 期主持和流程：读 `03_第2期_连接自己的经验_主持稿.md`。
   - 第 3 期主持和流程：读 `04_第3期_分享出去_主持稿.md`。
   - WorkBuddy 提示词：读 `05_WorkBuddy提示词库.md`。
   - 现场交付模板：读 `06_现场成果模板.md`。
   - 招募和海报文案：读 `07_招募文案与海报文案.md`。
   - 报名、反馈、复盘：读 `08_报名表与反馈表.md`。
   - 执行检查：读 `09_运营检查清单.md`。
   - 视觉设计：读 `10_海报设计简报.md`。
   - 课程内容细化：读 `11_课程内容设计稿.md`。

如果任务涉及飞书、外部知识库或发布平台，必须先确认用户是否明确授权，不要默认写入外部系统。

## 3. 内容边界

允许做的事：

- 优化活动流程、主持稿、提示词、模板、招募文案和运营清单。
- 把现场表达、知识资产卡、复盘记录整理成更清晰的资料。
- 基于已有定位新增草稿、成品、视觉说明或复盘文档。
- 帮主理人把经验沉淀成可复用的知识库条目。

不要做的事：

- 不要把活动改成纯 AI 工具课。
- 不要把活动改成卖课页或高压转化现场。
- 不要改成持续打卡、重运营社群或长期陪跑产品。
- 不要把主题泛化成个人 IP 大课，除非用户明确要求。
- 不要把 WorkBuddy 写成技术化功能介绍。
- 不要默认要求学员读完整本书。
- 不要强制学员公开发布内容。

判断标准：如果一个改动不能帮助学员在 2 小时现场带走可见成果，就不要放进正式文档。

## 4. 目录与产物输出规范

根目录保留正式主文档，使用 `编号_主题.md` 命名。当前 `00` 到 `11` 文件是主索引层和正式知识库，不要随意移动。

建议目录用途如下：

- `outputs/`：新增或改版后的正式交付文档，如新版主持稿、招募文案、复盘稿、对外发布稿。
- `drafts/`：AI 临时草稿、待确认版本、不同方案对比、结构重组前的新版本。
- `archive/`：被替换的旧版本、历史备份、过期方案。
- `assets/`：图片、海报、封面、二维码、可视化素材。
- `logs/`：修改记录、复盘记录、agent 操作摘要。

如果只是小幅优化正式文档，可以直接修改根目录主文档；如果会重写结构、改变定位或替换大段内容，先在 `drafts/` 生成草稿，确认后再进入正式文档。

## 5. 命名规则

正式主文档：

```text
编号_主题.md
```

草稿：

```text
drafts/YYYYMMDD_主题_版本.md
```

正式输出成品：

```text
outputs/YYYYMMDD_用途_标题.md
```

归档旧版：

```text
archive/YYYYMMDD_原文件名.md
```

修改记录：

```text
logs/YYYYMMDD_agent修改记录.md
```

命名要清楚表达用途，不使用“最终版”“最新版”这类容易失效的词。

## 6. Git 提交规范摘要

如果本次操作需要提交 Git，必须遵守 `AGENTS.md` 中的中文提交规范。

提交信息统一使用中文，格式为：

```text
动词 + 对象 + 目的
```

推荐动词包括：`新增`、`更新`、`修正`、`整理`、`忽略`、`初始化`。

示例：

```text
新增Agent统一启动入口
更新资料包使用说明
修正资料包清单遗漏
整理知识库产物目录
忽略临时输出文件
初始化AI读书会知识库工程
```

不要使用英文提交信息，不要使用 `update`、`fix`、`修改一下` 这类泛泛表述。每次提交只覆盖一类明确变更。如果仓库已经推送远端，默认不改写历史；用新的中文提交修正后续规范。

## 7. 文档修改逻辑

修改正式文档前，先判断任务类型：

### 内容增补

优先在原文对应章节下追加，不破坏原有结构。

适用例子：新增一个暖场问题、补充一段主持人口播、增加一个风险预案。

### 内容优化

保留原意、结构和活动定位，只优化表达。

适用例子：让文案更口语、让主持稿更像真人讲话、减少 AI 味。

### 结构重组

先在 `drafts/` 生成新版本，说明改动意图，再等待确认或明确指令后替换正式文档。

适用例子：重排整场流程、改三期主题、重写课程内容设计。

### 新增资料

先判断资料类型，再决定位置：

- 主持稿、总方案、提示词、模板、运营清单：适合进入根目录编号文档，或先放 `drafts/`。
- 招募文案、对外发布稿、活动复盘稿：适合放 `outputs/`。
- 海报、封面、二维码、图片：适合放素材类目录。
- 旧版本和弃用方案：适合放 `archive/`。
- 操作摘要和修改记录：适合放 `logs/`。

## 8. Agent 操作协议

Claude Code、Codex、WorkBuddy 在本项目中遵守同一套协议：

- 先读 `AGENTS.md`，再读本总纲，再读使用说明，再读任务相关文档。
- 不默认全量重写。
- 不新增未经用户说明的课程定位。
- 不把 WorkBuddy 写成技术工具说明。
- 不把活动讲成复杂知识库系统课。
- 不为了完整而增加现场执行负担。
- 所有新增内容都要服务“2 小时现场带走可见成果”。
- 修改正式文档时，尽量保留原文件风格、标题层级和表达口吻。
- 重要改动要能说明：改了哪里、为什么改、会影响哪些交付环节。

## 9. 三类 agent 的使用建议

### Claude Code

适合做结构化整理、批量文档重组、目录治理、版本归档和长文档一致性检查。

启动后先读取 `AGENTS.md`、本文件和 `00_使用说明.md`，再根据任务读取相关主文档。修改前先判断是否需要进入 `drafts/`。

### Codex

适合做本地文件创建、目录管理、批量检查、格式验证和可执行产物整理。

操作时优先保持文件结构稳定。涉及文件写入时，要明确产物位置，并在完成后检查文件是否存在、入口说明是否更新、目录是否符合规范。

### WorkBuddy

适合做提示词演练、主持稿润色、知识资产卡生成、学员成果改写和活动复盘。

使用 WorkBuddy 时，不需要解释复杂技术能力。把它当作“会陪你工作的 AI 小助手”，重点帮助用户抓重点、搭框架、出第一版，再由人判断和改写。

## 10. 质量检查清单

每次完成修改后，至少检查：

- 是否仍然围绕《1000个铁粉》三期线下 AI 读书演讲工作坊。
- 是否保留“2 小时现场带走成果”的交付标准。
- 是否避免了工具课、卖课页、重运营社群、泛个人 IP 课程的偏移。
- 是否明确产物放在哪里。
- 是否知道下一次 agent 进入项目时应该先读什么。
- 是否需要把旧版本归档。
- 是否需要补充修改记录。
- 如果产生 Git 提交，提交信息是否符合中文提交规范。

## 11. 典型任务处理示例

### 修改第 1 期主持稿

读取顺序：

1. `AGENTS.md`
2. `00_AGENT初始化总纲.md`
3. `00_使用说明.md`
4. `01_总方案.md`
5. `02_第1期_读懂一本书_主持稿.md`
6. 如涉及提示词，再读 `05_WorkBuddy提示词库.md`

处理逻辑：

- 小幅优化可直接修改 `02_第1期_读懂一本书_主持稿.md`。
- 大幅重写先放入 `drafts/YYYYMMDD_第1期主持稿_重组版.md`。

### 新增一版朋友圈招募文案

读取顺序：

1. `AGENTS.md`
2. `00_AGENT初始化总纲.md`
3. `00_使用说明.md`
4. `01_总方案.md`
5. `07_招募文案与海报文案.md`

处理逻辑：

- 如果是正式可发布版本，放入 `outputs/YYYYMMDD_招募文案_朋友圈.md`。
- 如果是多个方向对比，放入 `drafts/YYYYMMDD_朋友圈招募文案_方案对比.md`。

### 归档一版旧海报文案

读取顺序：

1. `AGENTS.md`
2. `00_AGENT初始化总纲.md`
3. `00_使用说明.md`
4. `07_招募文案与海报文案.md`
5. `10_海报设计简报.md`

处理逻辑：

- 旧版文案复制或移动到 `archive/YYYYMMDD_旧海报文案.md`。
- 新版对外物料放入 `outputs/` 或继续更新对应正式主文档。
- 海报图片和封面素材保留在素材类目录，并在必要时补充索引说明。
