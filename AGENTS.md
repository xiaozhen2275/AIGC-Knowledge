# AGENTS.md：AI读书会知识库统一启动入口

本文件是当前项目的最高优先级 Agent 入口。Claude Code、Codex、WorkBuddy 以及其他 AI agent 进入本项目后，都以本文件为统一中控规则。

## 1. 启动顺序

所有 agent 在读取、整理、修改、扩展本项目资料前，必须按以下顺序加载上下文：

1. 先读 `AGENTS.md`。
2. 再读 `00_AGENT初始化总纲.md`。
3. 再读 `00_使用说明.md`。
4. 根据任务类型读取对应主文档。

如果任务涉及飞书、外部知识库或发布平台，必须先确认用户是否明确授权，不要默认写入外部系统。

## 2. Agent 类型入口

### Claude Code

Claude Code 进入项目后，如果优先读取到 `CLAUDE.md`，请继续跳转并遵守本文件。

适合任务：

- 文档结构整理
- 批量内容重组
- 目录治理
- 版本归档
- 长文档一致性检查

工作规则：

- 先读本文件，再读 `00_AGENT初始化总纲.md`。
- 结构性重写先放入 `drafts/`，不要直接覆盖正式主文档。
- 修改前先判断任务属于内容增补、内容优化、结构重组还是新增资料。

### Codex

Codex 进入项目后，以本文件作为项目规则入口。

适合任务：

- 本地文件创建和修改
- 目录创建和检查
- 批量搜索、校验和格式检查
- 可执行产物整理

工作规则：

- 涉及写入时，必须明确产物应该进入根目录、`outputs/`、`drafts/`、`archive/`、`assets/` 还是 `logs/`。
- 完成后检查文件是否存在、入口链路是否正确、目录是否符合规范。
- 不要移动当前 `00` 到 `11` 的正式主文档，除非用户明确要求。

### WorkBuddy

WorkBuddy 进入项目后，如果不能自动读取本地文件，请把 `WorkBuddy_启动提示词.md` 作为开场提示词，并要求它遵守本文件与 `00_AGENT初始化总纲.md`。

适合任务：

- 提示词演练
- 主持稿润色
- 知识资产卡生成
- 学员成果改写
- 活动复盘整理

工作规则：

- 不要把 WorkBuddy 写成技术工具说明。
- 用小白能懂的语言解释 AI 辅助阅读和表达。
- 重点帮助用户抓重点、搭框架、出第一版，再由人判断和改写。

## 3. 任务文档入口

按任务读取对应文档：

- 总体设计：`01_总方案.md`
- 第 1 期主持和流程：`02_第1期_读懂一本书_主持稿.md`
- 第 2 期主持和流程：`03_第2期_连接自己的经验_主持稿.md`
- 第 3 期主持和流程：`04_第3期_分享出去_主持稿.md`
- WorkBuddy 提示词：`05_WorkBuddy提示词库.md`
- 现场交付模板：`06_现场成果模板.md`
- 招募和海报文案：`07_招募文案与海报文案.md`
- 报名、反馈、复盘：`08_报名表与反馈表.md`
- 执行检查：`09_运营检查清单.md`
- 视觉设计：`10_海报设计简报.md`
- 课程内容细化：`11_课程内容设计稿.md`

## 4. 统一知识库规则

项目定位、内容边界、目录规范、命名规则、文档修改逻辑，以 `00_AGENT初始化总纲.md` 为准。

关键原则：

- 本项目是三期线下 AI 读书演讲工作坊资料包，不是普通读书笔记仓库。
- 不把活动改成纯 AI 工具课、卖课页、重运营社群或泛个人 IP 大课。
- 不默认全量重写正式文档。
- 所有新增内容都要服务“2 小时现场带走可见成果”。
- 重大结构调整先进入 `drafts/`，确认后再替换正式文档。

## 5. 产物目录

- 根目录：正式主文档，当前 `00` 到 `11` 文件不要随意移动。
- `outputs/`：新增或改版后的正式交付文档。
- `drafts/`：AI 临时草稿、待确认版本、不同方案对比。
- `archive/`：被替换的旧版本、历史备份。
- `assets/`：图片、海报、封面、二维码、可视化素材。
- `logs/`：修改记录、复盘记录、agent 操作摘要。

## 6. Git 提交规范

所有提交信息必须使用中文，格式为：

```text
动词 + 对象 + 目的
```

推荐动词：

- `新增`：新增文档、目录、配置或素材索引。
- `更新`：调整已有内容、入口说明或规范。
- `修正`：修复遗漏、错误、命名或文档不一致。
- `整理`：目录治理、归档、结构梳理。
- `忽略`：更新 `.gitignore` 或移除中间产物跟踪。
- `初始化`：首次建立仓库或工程骨架。

示例：

```text
新增Agent统一启动入口
更新资料包使用说明
修正资料包清单遗漏
整理知识库产物目录
忽略临时输出文件
初始化AI读书会知识库工程
```

提交约束：

- 不使用英文提交信息。
- 不使用泛泛的 `update`、`fix`、`修改一下`。
- 不在 commit message 里写过长说明；详细说明放文档或日志。
- 每次提交只覆盖一类明确变更。
- 如果仓库已经推送远端，默认不改写历史；用新的中文提交修正后续规范。

## 7. 完成检查

每次完成修改后，检查：

- 是否仍然围绕《1000个铁粉》三期线下 AI 读书演讲工作坊。
- 是否保留“2 小时现场带走成果”的交付标准。
- 是否明确产物放置目录。
- 是否需要归档旧版本。
- 是否保持 `AGENTS.md` → `00_AGENT初始化总纲.md` → `00_使用说明.md` → 任务文档的启动链路。
- 如果产生 Git 提交，commit message 是否符合中文提交规范。
